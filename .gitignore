# Runtime data — never commit the live database or logs
backend/dxmap.db
backend/dxmap.db-wal
backend/dxmap.db-shm
backend/improvements.log

# Incident data — the code that renders/loads it is open source, the actual
# incident data (seed data, exports, or anything derived from real reports)
# is not. See NEXT-STEPS.md and DEVELOPMENT-WORKFLOW.md for why.
frontend/data/

# Secrets — never commit API keys or environment files
.env
*.env
deploy/.env

# Python
__pycache__/
*.pyc
.pytest_cache/
venv/
.venv/

# OS / editor cruft
.DS_Store
*.swp

# frontend rewrite
web/node_modules/
web/dist/
web/*.tsbuildinfo

.impeccable/
.claude/skills/
web/.impeccable/
